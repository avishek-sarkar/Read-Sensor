# Read Sensor

Read Sensor is a simple Android application built with Java that demonstrates how to read data from the device's accelerometer sensor. The app displays the X, Y, and Z axis values of the accelerometer in real-time.

## Features

*   Reads accelerometer data from the device.
*   Displays the X, Y, and Z axis values in a clean and simple UI.
*   Elegant dark theme.
*   Custom app icon.

## Building the project

To build the project, you can follow these steps:

1.  **Clone the repository:**
    ```bash
    git clone https://github.com/avishek-sarkar/Read-Sensor.git
    ```

2.  **Open in Android Studio:**
    Open the cloned project in Android Studio.

3.  **Build the project:**
    Let Gradle sync the project and then build it.

4.  **Run the app:**
    Run the app on an emulator or a physical Android device.

## Release

You can download the latest release of the app from the [releases page](https://github.com/avishek-sarkar/Read-Sensor/releases/tag/v1.0.0).
